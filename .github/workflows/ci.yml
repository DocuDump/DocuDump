name: ci

on: [push]

jobs:
    lint:
        runs-on: ubuntu-22.04

        steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-node
              with:
                  node-version: "20.x"

            - run: npm ci

            - run: ./lint.sh
